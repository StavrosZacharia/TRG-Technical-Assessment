apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: flask-hello-world
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: flask-hello-world
          port: 8080
